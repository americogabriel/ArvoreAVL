# ArvoreAVL
Um programa que demonstra a incrementação da estrutura de uma árvore AVL no código de forma clara e documentada. O programa é feito em C++ utilizando pontos como POO com classes e métodos, alocação de memória, ponteiros e estruturas basicas de lógica(if,else if,else,while)
